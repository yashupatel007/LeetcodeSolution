class Solution {
public:
    vector<int> findArray(vector<int>& pref) {
        // A ^ B = C then A ^ C = B
        int n=pref.size();
        for(int i=n-1;i>=1;i--)
        {
            pref[i]=pref[i] ^ pref[i-1];
        }
        return pref;
    }
};
